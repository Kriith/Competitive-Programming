#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define FOR(i, a, b) for(int i = a; i<b; i++)
void solve(){
    int n;
    cin>>n;
    if(n<3){
        cout<<"0\n";
        return;
    }
    int a[n];
    FOR(i, 0, n)
        cin>>a[n];
    sort(a, a+n);
    FOR(i, 0, n-3){
        if()
    }
}
int main(){
    int t;
    cin>>t;
    while(t--)
        solve();
    return 0;
}